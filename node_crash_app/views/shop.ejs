<%- include('includes/header.ejs') %>
        <main>
            <p>Product List</p>
            <% if (products.length> 0) { %> <% products.forEach(item=> { %>
            <p><%= item.title %></p>
            <% }) %> <% } else{ %>
            <p>Product Not found</p>
            <% } %>
        </main>
    </body>
</html>
